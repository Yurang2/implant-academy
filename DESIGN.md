# 레벨 디자인 — 링고 아카데미

이 문서가 이 앱의 학습 경험 설계의 단일 출처입니다. 여기 적힌 규칙 중 기계로 강제할 수
있는 것은 전부 `check.mjs`가 강제하고(§7 매핑표), 엔진 동작에 관한 것은 `app.html`이
구현합니다. 규칙을 바꾸려면 **이 문서 → check.mjs → 데이터** 순서로 바꾸세요.

레퍼런스: 듀오링고(경로·하트·오답 재출제·게임화), 스픽(표현 선노출·듣고 따라 말하기 중심).

---

## 1. 학습 경험 원칙

1. **가르치기 전에 시험하지 않는다.** 레슨이 시작되면 '오늘의 표현' 카드가 그 레슨에서
   조립(wordbank)하고 말하게(speak) 될 문장을 먼저 보여 주고 들려준다.
2. **인식이 산출보다 먼저다.** 듣고 고르는 것이 가장 쉽고, 문장을 조립하는 것이 그다음,
   입으로 말하는 것이 가장 어렵다. 문항은 항상 이 순서로 나온다 (§2).
3. **모든 오답은 즉시 교정된다.** 오답 즉시 정답·해설을 보여 주고, 그 문항은 레슨 끝에
   맞힐 때까지 재출제된다 (match 제외 — §5).
4. **한 레슨은 한 호흡(3~5분).** 6문항 고정. 늘리고 싶으면 레슨을 쪼갠다.
5. **듣기가 중심이다.** 레슨마다 듣기 1개가 필수이고, 정답을 맞히면 목표 문장을 한 번 더
   들려준다. 말하기(speak)는 선택 — 음성 인식은 브라우저별 편차가 커서 학습의 필수 관문으로
   삼지 않는다. 음성 기능이 없는 환경에서는 해당 문항을 빼되 레슨은 막히지 않는다 (§6).
6. **아직 안 배운 문법은 쓰지 않는다.** 이것이 상황별 표현집과 학습 과정을 가른다 (§3.5).

## 2. 레슨 해부 (6문항 템플릿)

모든 레슨은 정확히 6문항, 유형 중복 없음, 다음 단계 순서를 따른다:

| 단계 | 유형 | 역할 |
|---|---|---|
| 0 입력 | `listen` | 목표 문장을 귀로 먼저 만난다 (필수, 항상 1번) |
| 1–3 인식·판단 | `match` → `choice` → `ox` | 어휘 짝·의미 인식·용법 판단 |
| 4 문형 | `blank` | 문장 구조의 빈 곳 하나에 집중 |
| 5 조립 | `wordbank` | 타일로 문장 전체를 산출 (필수, speak 없는 레슨의 산출 정점) |
| 6 산출 | `speak` | 입으로 말한다 (선택 — 아래 정책 참고) |
| 7 담화 | `order` | 문장을 넘어 대화 흐름 (유닛 마지막 레슨의 캡스톤) |

- 필수 2유형: `listen`, `wordbank` — 레슨마다 정확히 1개씩.
- **`speak`는 선택 유형이다.** 음성 인식(STT) 의존을 늘리지 않는 제품 정책에 따라
  U6부터는 speak 없이 저작한다 (신규 레슨 구성: L1~L3 = listen·match·choice·ox·blank·wordbank,
  L4 = listen·match·choice·blank·wordbank·order). U1~5의 기존 speak는 유지하되,
  인식이 안 되는 환경에서는 자동 제외된다 (§6).
- 나머지 슬롯은 `match / choice / ox / blank / order` 중에서 채운다 (유형 중복 금지).
- **각 유닛의 L4는 `order` 캡스톤으로 끝난다** — 유닛에서 배운 문장들을 대화 순서로 재구성.
- 단계 번호가 역행하는 배치는 check.mjs가 **오류**로 거부한다.

## 3. 레벨 밴드와 난이도 램프

커리큘럼은 **레벨 밴드**로 나뉘고, 유닛은 반드시 밴드 순서대로 배치된다(역행은 오류).

| 밴드 | 이름 | 담당 문법 | 문장 길이 상한 |
|---|---|---|---|
| A1.1 | 영어의 뼈대 | be동사, 인칭대명사, 관사, 복수형 | 7단어 |
| A1.2 | 매일의 동작 | 일반동사 현재, 3인칭 -s, do/does | 9단어 |
| A1.3 | 지금, 여기 | 현재진행형, 장소·시각 전치사 | 11단어 |
| A2.1 | 지난 일 말하기 | be동사 과거, 규칙·불규칙 과거, did | 12단어 |
| A2.2 | 앞일과 의무 | will, be going to, should·have to·must | 13단어 |
| A2.3 | 견주어 말하기 | 비교급·최상급, 수량 표현 | 14단어 |
| B1.1 | 문장을 잇다 | 현재완료, 관계대명사 | 16단어 |

- 상한은 listen.audio / speak.text에 적용되며 초과는 **오류**다.
- wordbank 정답은 2~9단어. order 항목은 3~5문장, 각 24자 이내(경고).
- 각 밴드는 **문법 유닛(그 문법을 가르침) → 상황 유닛(그 문법을 써먹음)** 순으로 배치한다.

## 3.5 문법 축 — "체계적"의 기계 장치

상황별 표현집과 언어 학습 과정을 가르는 것은 이것 하나다: **아직 배우지 않은 문법은
쓰지 않는다.** check.mjs가 각 문항의 영어를 검사해, 그 유닛의 밴드보다 뒤에서 도입되는
구조가 쓰이면 **오류로 거부**한다.

감시 대상 구조와 도입 밴드는 `check.mjs`의 `GRAMMAR` 배열에 있다(현재진행형 → A1.3,
과거시제 → A2.1, will·의무조동사 → A2.2, 비교급 → A2.3, 현재완료·관계대명사 → B1.1).

예외는 두 가지뿐이다:
- `GRAMMAR_EXEMPT` — "Nice to meet you"처럼 문법이 아니라 통째로 익히는 굳어진 인사말.
- 문항의 `allow: ["미래 will"]` — 그 표현을 문법이 아니라 **관용구로 가르칠 때만**.
  예: 주문할 때의 "I'll have the same." 해설에 "지금은 통째로 외우세요"라고 밝혀야 한다.

이 두 장치를 남용하면 축이 무의미해진다. 새 문항이 축에 걸리면 **먼저 문장을 고치고**,
정말 관용구일 때만 allow를 쓴다.

### 어휘 재노출
핵심어는 여러 번 만나야 남는다. 한 번만 등장하는 어휘 비율을 35% 이하로 유지한다
(현재 35%). 밴드를 넘어 같은 표현을 다시 쓰는 것은 **낭비가 아니라 나선형 복습**이며,
dupcheck는 이를 중복으로 세지 않는다(§7).

## 4. 문항 콘텐츠 규칙

- `listen` 오답 보기는 **문법적인 문장 + 소리로 헷갈릴 만한 것**(최소대립쌍 우선).
  비문이면 듣지 않고도 소거된다. 보기 4개는 정규화(소문자·구두점 제거) 후에도 서로 달라야 한다(오류).
- `wordbank` 정답은 구두점 없는 타일 문자열. **extra 타일이 또 다른 자연스러운 문장을
  만들 수 있으면 안 된다.** 불가피하게 자연스러운 대체 문장이 생기면 `accept` 배열로
  정답 인정한다 (accept는 타일로 조립 가능해야 하며, check.mjs가 검증).
- `ox` 정답은 track 전체에서 참:거짓이 30~70% 안에 있어야 한다(경고).
- `match` 왼쪽 열은 학습 언어(영어), 오른쪽은 뜻(경고).
- 모든 문항에 `explanation` 필수. 해설의 언어학적 주장은 사실이어야 한다 — 콘텐츠 QA 대상.
- **정답 위치는 저작 규약으로 `answerIndex: 0`에 둔다.** 화면 순서는 엔진이 렌더할 때마다
  섞는다. 데이터에서 위치를 다양화하려 애쓰지 말 것 (검증이 단순해지고 편향은 원천 차단됨).

## 5. 진행·보상 파라미터

| 파라미터 | 값 | 근거 |
|---|---|---|
| 하트 | 레슨당 5, 오답 시 −1, 소진 시 재도전 | 실수 허용 폭 + 긴장감 (듀오링고 동일) |
| 오답 재출제 | 레슨 끝에 맞힐 때까지 | 교정 없는 오답 금지 |
| XP (본학습) | 첫 시도 정답 +10 / 재시도 +5 / 퍼펙트 +20 | |
| XP (복습) | 첫 시도 +5 / 재시도 +2 / 퍼펙트 +10 | 같은 레슨 반복으로 XP 갈기 방지 |
| 복습 순서 | 문항 순서 셔플 | 답 위치·순서 암기 무력화 |
| 별점 | 첫시도 정답률 ≥90% ★3 / ≥70% ★2 / 그 외 ★1 | 기록은 최고치 유지 |
| 발음 통과선 | 목표 단어의 70% 인식 | 인식기 오차를 감안한 관대한 선 — 시도 유도가 목적 |
| 발음 채점 | 단어 포함률(순서 무시), 축약형 전개 후 비교 | 인식기가 어순·축약을 흔들기 때문 |
| `match` 특례 | 오답 시 하트·재출제 없음, 완료 시 +5 (첫 시도 무실수면 정답 취급) | 탐색형 과제 — 시행착오가 곧 학습 (듀오링고 동일) |
| 스트릭 | 하루 1레슨 완료로 유지, 하루 공백까지 인정 | |

## 6. 건너뛰기·음성 미지원 정책

- **말하기 건너뛰기("지금은 말하기 어려워요")는 정답이 아니다.**
  해당 문항을 채점 모수에서 제외하고 XP 0. 정답률·퍼펙트는 남은 문항 기준으로 계산한다
  (듀오링고의 "지금은 말할 수 없어요"와 같은 의미론). 건너뛴 개수는 결과 화면에 표시.
- TTS가 없으면 `listen` 문항을, 음성 인식이 없거나 마이크를 끄면 `speak` 문항을
  **세션에서 아예 제외**한다. 답을 글로 보여 주며 만점 처리하는 것보다 정직하다.
- 마이크 권한 거부·무음 등 일시 오류는 문항 안에서 재시도 안내, 영구 오류(network 등)는
  그 자리에서 건너뛰기 경로로 전환.

## 6.5 오답 노트

- **기록**: match(탐색형)를 제외한 모든 오답이 사용자별 노트에 문항 ID로 쌓인다.
  레슨을 중도 이탈해도 이미 기록된 오답은 남는다.
- **해제**: 어떤 세션에서든 그 문항을 **첫 시도에** 맞히면 노트에서 지워진다.
  재시도로 맞힌 것은 지워지지 않는다 (아직 약한 문항).
- **세션**: 홈의 오답 노트 카드로 시작. 많이 틀린 문항 우선으로 최대 6문항, 순서 셔플,
  하트 5개, XP는 복습 배율(+5/+2, 퍼펙트 +10). 레슨 완료 기록·별점은 남기지 않는다.
- 문항 ID는 **이름 기반**이다: `트랙:유닛slug/레슨slug + q번호`. 위치가 아니라 이름에
  묶여 있으므로 유닛을 몇 번을 재배치해도 진도·오답 기록이 어긋나지 않는다.
  옛 위치 기반 ID(`english:u3l2`)는 레슨의 `legacy` 필드를 통해 앱이 자동 변환한다.
- 여전히 위험한 것은 **slug를 바꾸거나 레슨 내 문항 순서를 바꾸는 것**이다. 문항은
  삽입·삭제보다 교체(내용만 수정)를 우선한다.

## 6.6 클라우드 동기화

- **오프라인 우선**: 모든 기록은 먼저 localStorage에 저장된다. 동기화는 그 위의
  부가 계층이며, 서버 주소·동기화 코드가 설정된 경우에만 동작한다.
- 저장이 일어날 때마다 디바운스(1.5초) 후 서버로 push, 로그인 시 pull 후 병합,
  오프라인 복귀(online 이벤트) 시 push.
- **슬롯 키** = SHA-256(동기화 코드 + "|" + 이름). 코드 원문은 전송되지 않는다.
- **병합 규칙**: XP·별점·레슨 진도는 max, 오답 노트는 합집합(로컬 우선),
  스트릭은 lastDay가 최근인 쪽. 알려진 한계: 두 기기 동시 학습 시 XP 일부 소실 가능,
  한 기기에서 지운 오답이 병합으로 되살아날 수 있음 — v1 트레이드오프로 수용.
- 서버 레퍼런스 구현은 `sync-worker.js` (Cloudflare Worker + KV), 배포법은 DEPLOY.md.

## 7. 규칙 → 기계 강제 매핑

| 규칙 | 강제 위치 | 수준 |
|---|---|---|
| 레슨 6문항·유형 중복 금지·필수 유형(listen·wordbank) | check.mjs | 오류 |
| 단계 순서 (listen→…→wordbank→speak→order) | check.mjs | 오류 |
| **문법 축 — 미도입 문법 사용 금지** | check.mjs | 오류 |
| 유닛 slug·band 존재, 밴드 순서 역행 금지 | check.mjs | 오류 |
| 밴드별 문장 길이 상한 | check.mjs | 오류 |
| listen 정답=음원 일치, 보기 정규화 중복 | check.mjs | 오류 |
| wordbank accept 조립 가능성 | check.mjs | 오류 |
| ox 편향, match 좌열 언어, 항목 길이 | check.mjs | 경고 |
| 유닛 내 같은 유형 지문 중복 | check.mjs | 경고 |
| 같은 밴드 내 중복 문항 | dupcheck.mjs | 리포트 |
| 보기 셔플·스킵 채점·복습 XP·재진입 가드·ID 마이그레이션 | app.html + 브라우저 테스트 | 테스트 |

dupcheck는 나선형 커리큘럼에 맞춰 세 가지를 중복으로 세지 않는다: **같은 유닛 안의
평서문↔의문문↔부정문 변형**(그 유닛의 목적), **내용어 3개 미만**(유사도가 무의미),
**밴드를 넘는 재등장**(의도된 복습). 긍정↔부정 대조 쌍은 따로 분류해 보고만 한다.

## 8. 로드맵 (다음 반복 후보 — 우선순위순)

1. **배치 진단**: 첫 방문 시 5문항 진단으로 시작 밴드 결정 — 22유닛을 순서대로만
   가야 하는 지금 구조에서 이미 아는 사람에게 가장 큰 마찰이다.
2. **일일 목표**: 하루 XP 목표(기본 30)와 달성 링 — 스트릭과 연결.
3. **문법 노트 화면**: 밴드 시작 지점에 그 밴드의 문법을 설명하는 읽기 화면
   (듀오링고 Tips에 해당). 지금은 해설이 유일한 설명 통로다.
4. **간격 반복 고도화**: 오답 노트에 망각 곡선 기반 재출제 시점(1일/3일/7일) 도입.
5. **B1 확장**: 수동태, 조건문 if, 간접의문문 — B1.1이 아직 2유닛뿐이다.
6. **컬러·사진 트랙**: 색상 이론을 한국어로 배우는 독립 트랙. 색 견본(swatch)·
   슬라이더(mixer)·대비 판정(contrast) 문항 유형 신설이 선행되어야 한다.

구현 완료되어 로드맵에서 내려간 것: 오답 노트(§6.5), 클라우드 동기화(§6.6),
문법 축과 레벨 밴드(§3, §3.5).
정책상 제외된 것: 스픽식 섀도잉 모드 — 음성 인식 의존을 늘리지 않기로 함.
